<?php require 'vendor/autoload.php'; $app = require_once 'bootstrap/app.php'; $kernel = $app->make(Illuminate\Contracts\Console\Kernel::class); $kernel->bootstrap(); $order = App\Models\Order::latest()->first(); echo $order->created_at . ' - ' . $order->estimated_serve_time . ' - ' . $order->order_status;
